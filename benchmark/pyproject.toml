[project]
name = "fastermcp-benchmark"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0",
    "mcp>=1.0",
    "fastermcp",
]

[tool.uv.sources]
fastermcp = { path = "../python" }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
