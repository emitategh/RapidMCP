"""Generated protobuf stubs. Adds this directory to sys.path so grpc_tools
bare imports (e.g. 'import mcp_pb2') resolve correctly."""
import os
import sys

_dir = os.path.dirname(os.path.abspath(__file__))
if _dir not in sys.path:
    sys.path.insert(0, _dir)
